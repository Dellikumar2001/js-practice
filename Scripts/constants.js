//Constants the value can't be changed

const name = 1; //any values can be assigned

const UNAME = "delli@mail.com";
const PWD = "thisIsPwd";

const WHITE_COLOUR = "#fff"
const BLACK_COLOUR = "#000"// LATER WE can use it in UI

// login(UNAME, PWD);

const DATE_OF_BIRTH = "01.05.2001";
//const age = calAge(DATE_OF_BIRTH);  LATER WE WILL STUDY
